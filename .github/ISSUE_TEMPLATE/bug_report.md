---
name: Bug report
about: Something in the CLI, a package, or the shell doesn't work
labels: bug
---

**What happened**

**What you expected**

**Repro**
- `appcask` version (`appcask --version`):
- Node version:
- OS / device:
- A minimal `appcask.config.json` (redact URLs if needed):

```json

```

**`appcask doctor` output** (if relevant)

```

```
