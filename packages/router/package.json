{
  "name": "@appcask/router",
  "version": "0.1.0",
  "description": "Pure URL routing for the appcask shell: decide whether a navigation stays in the WebView, opens a Custom Tab, hands off to the OS auth browser, or goes to the system. Zero dependencies.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": "./dist/index.js" },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "eslint src"
  },
  "devDependencies": {
    "@appcask/config": "workspace:*"
  }
}
