<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    </style>

    <!-- Launch / splash theme (Android 12+ SplashScreen API, via core-splashscreen).
         The splash icon is the launcher icon; `appcask android` fills
         @color/appcask_splash_background from the config theme. -->
    <style name="AppTheme.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/appcask_splash_background</item>
        <item name="postSplashScreenTheme">@style/AppTheme</item>
    </style>

</resources>
